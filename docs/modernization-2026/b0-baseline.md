# B0 — Reproducible historical Django baseline

## Status

B0 is observational. It does not change application behavior, models, migrations, templates, static assets, database contents or dependency authority.

Historical/current code authority:

`b2fc80368aa0749ca005d4efc508f6ba638083b4`

The 17 inspected commits are authored by Enzo Pinotti and were concentrated on 2025-04-14. The README identifies the repository as **Actividad 0 — Introducción a Git y Django**, Ingeniería de Software, UTN-FRLP.

## Historical runtime evidence

The repository does not declare Python or Django versions, but two independent repository artifacts identify the original environment:

- tracked bytecode is named `*.cpython-312.pyc` → CPython 3.12;
- `polls/migrations/0001_initial.py` says `Generated by Django 5.0 on 2025-04-14 21:35`.

B0 therefore reproduces the project on **Python 3.12 + Django 5.0** rather than guessing a current framework version.

Measured runner runtime:

- Python `3.12.14`;
- Django `5.0`;
- installed Django transitive dependencies: `asgiref 3.12.1`, `sqlparse 0.6.0`.

## Repository structure

Project:

`trabajocero`

Application:

`polls`

Maintained routes:

- `/polls/`;
- `/polls/<pk>/`;
- `/polls/<pk>/results/`;
- `/polls/<question_id>/vote/`;
- `/admin/`.

Models:

- `Question(question_text, pub_date)`;
- `Choice(question, choice_text, votes)`.

The current code follows the Django polls tutorial shape: generic list/detail/results views plus a function-based vote handler.

## Django checks

Historical Django 5.0 reproduction:

- `python manage.py check`: **0 issues**;
- `python manage.py makemigrations --check --dry-run`: **No changes detected**;
- migration graph loads successfully;
- existing migration authority is internally coherent with the current models.

The deployment-oriented check reports seven expected development-configuration warnings:

- no HSTS;
- no SSL redirect;
- generated/insecure development secret;
- session cookie not secure;
- CSRF cookie not secure;
- `DEBUG=True`;
- empty `ALLOWED_HOSTS`.

These are evidence that the repository is a local learning project, not proof that B0 should convert it into production infrastructure.

## Existing tests

The historical command:

```bash
python manage.py test -v 2
```

reports:

- **Found 0 test(s)**;
- **Ran 0 tests**.

`polls/tests.py` contains only Django's generated `TestCase` import/comment.

B2 owns behavior tests.

## HTTP/runtime smoke

Using the tracked SQLite database and Django development server:

- `/polls/` → **200**;
- `/static/polls/style.css` → **200**;
- `/admin/` → **302** for anonymous access;
- first tracked question detail → **200**;
- first tracked question results → **200**.

This demonstrates that the historical application is runnable, its templates/static CSS resolve, and the tracked data is sufficient to exercise the visible polls flow.

## Tracked SQLite state

`trabajocero/db.sqlite3` is committed.

B0 inspected **metadata/counts only**, never row values.

Measured database metadata:

- size: `143360` bytes;
- SHA-256: `9efc9d047647bc20dd83142f260f48c70aab37a0b60dba57d52d6a91fc5c00c3`;
- polls questions: **3**;
- polls choices: **5**;
- auth users: **1**;
- Django migrations recorded: **19**;
- Django sessions: **2**.

Because the database contains an auth user and sessions, B1 must treat the committed SQLite file as historical/runtime state and remove it from maintained authority unless a concrete fixture requirement is proven. Its row contents must not be copied into documentation or logs.

## Generated artifacts are tracked

The repository tracks:

- `db.sqlite3`;
- 12 CPython 3.12 `.pyc` files under `__pycache__`.

Simply importing/running Django under Python 3.12.14 rewrites all **12 tracked bytecode files** while leaving source and database content unchanged.

This makes an otherwise successful historical run produce a dirty Git working tree.

B1 should therefore remove Python bytecode/cache artifacts from maintained Git authority and ignore them permanently.

## Dependency/security baseline

There is no:

- `requirements.txt`;
- `pyproject.toml`;
- dependency lock;
- declared supported runtime.

B0 audited the historically evidenced direct target `Django==5.0` without adding it to the repository.

The audit exits non-zero and reports multiple current advisories against Django 5.0. The raw audit contains duplicate/aliased advisory entries; the B0 probe now deduplicates findings before presenting them.

This means Django 5.0 is valid **historical reproduction evidence**, but it is not a defensible maintained 2026 dependency target.

B3 owns framework selection after tests exist.

## B0 evidence

Initial full measurement run:

- workflow run: `35362640285`;
- measured SHA: `4980becbcd040660ce6bed902fa4df955f9aba2f`;
- evidence artifact: `10555412447`;
- artifact digest: `sha256:6d922ffc1a67fe5b23c6b4c6a9e4aacf57adfbc7192a9026bb484f761ad4c305`.

The run reached every application/audit probe successfully. Its final job result was failure only because the first guard treated the 12 expected tracked `.pyc` rewrites as an unexpected mutation. The follow-up B0 workflow converts that behavior into an explicit baseline assertion: only those 12 tracked bytecodes may change.

## B0 exit assessment

B0 establishes:

- exact historical/current code authority;
- clear authorship/provenance;
- evidence-backed Python/Django baseline;
- reproducible Django startup;
- clean system/model migration relationship;
- real HTTP smoke;
- test count = 0;
- tracked SQLite state characterized without exposing values;
- tracked bytecode mutation problem characterized;
- dependency/security debt measured;
- no application behavior changes.

## Next block

B1 should establish reproducible dependency/runtime authority and repository hygiene before writing behavioral tests.

The smallest justified B1 scope is:

1. add a Python/Django dependency authority suitable for the selected maintenance line;
2. add `.gitignore`;
3. remove tracked `__pycache__/*.pyc`;
4. remove the stateful tracked SQLite database from maintained authority while preserving migrations as schema truth;
5. keep local database creation via migrations;
6. add a deterministic read-only quality/check gate;
7. do not change polls behavior yet.
